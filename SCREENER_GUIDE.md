"""
SCREENER VIEW - USAGE GUIDE

New Features Added:
====================

1. COLUMN SORTING
   - Click any column header to sort by that column (A-Z or 0-9)
   - Click the same header again to reverse the sort order
   - Shows indicator in status bar which column is sorted

2. FILTERING
   - SECTOR/REGION filters: Check/uncheck boxes at top of screener
   - INDUSTRY filters: Check/uncheck boxes at top of screener
   - Filters apply in real-time as you change them
   - All filters are ON by default when you load the screener

3. DOUBLE-CLICK TO VIEW DETAILS
   - Double-click any row to view detailed historical data for that ticker
   - Switch back to screener view with the "SCREENER" tab button

Keyboard/Mouse:
- Click column header → Sort by that column
- Click again → Reverse sort direction
- Check/uncheck filters → Instant refresh
- Double-click row → View historical details
- Horizontal scroll → View all columns

Example Workflow:
1. Open screener view
2. Uncheck "Technology" sector to hide tech stocks
3. Click "Z-Score" column to sort by bankruptcy risk (low risk = high Z-Score)
4. Click "Z-Score" again to see riskiest stocks first
5. Double-click a stock to view its historical metrics
"""
